{{-- Separador flexible: empuja hacia abajo el contenido siguiente dentro de un
     contenedor flex en columna. Clase REAL de Bootstrap: mt-auto (NO 'flex-1' de Tailwind).
     Fusiona las clases del consumidor en el único class del raíz. --}}
<div {{ $attributes->class(['mt-auto']) }}></div>
